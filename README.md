# FullGame

## Quick Launch

To make the game easy to find, use the launcher in the repository root:

- `LaunchGame.bat`

It will look for `ThirdPersonSandbox.exe` in the default CMake output paths and run it.

## Build

```bat
cmake -S . -B build
cmake --build build --config Debug
```
